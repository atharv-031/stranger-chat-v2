{
  "name": "stranger-chat-v2",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "helmet": "^7.0.0",
    "xss-clean": "^0.1.4",
    "express-rate-limit": "^7.0.0"
  }
}
